/**
 * @summary
 * External (public) API routes for Version 1.
 * Handles unauthenticated endpoints.
 *
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

export default router;
